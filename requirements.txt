# Required
requests>=2.28.0

# Optional — only needed for LLM-based therapy area classification (Claude Haiku).
# If ANTHROPIC_API_KEY env var is not set, the script uses keyword classification automatically.
# Install manually when ready: pip install anthropic>=0.39.0
anthropic>=0.39.0
